import LoginPageClient from "./page-client.jsx";

export const metadata = {
  title: "Login",
  description:
    "UMATTR access is handled through the product routes and Payhip delivery.",
};

export default function LoginPage() {
  return <LoginPageClient />;
}
